import { FileArguments } from './types';
export declare const secondArgumentNotSupplied: (inputArguments: string[]) => boolean;
export declare const parseInputArguments: (inputArguments: string[]) => FileArguments;
